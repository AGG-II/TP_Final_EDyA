#include "parser/parser.h"
#include <stdio.h>
#include <stdlib.h>

int main(int argc, char **argv) {
  FILE *diccionario, *oraciones, *correcciones;
  int resultado = 0;
  if (resultado) parser_espaciar(diccionario, oraciones, correcciones);
  return 0;
}